int main(void)
{
    // plaintext to cipher text

    // must accept single command line argument

    // must contain non negative integer (k)

    // print error and return if not correct

    // convert argv from srtring to int

    // prompt for plain text

    // for each char, rotate by key
}